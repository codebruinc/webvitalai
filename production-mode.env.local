# WebVitalAI Production Environment Configuration
# Copy this file to .env.local to run in production mode

# IMPORTANT: These settings disable test data and enable real scan results
NODE_ENV=production
TESTING_MODE=false

# Keep your existing configuration values below
# Only the NODE_ENV and TESTING_MODE settings need to be changed